[package]
name = "tauri-plugin-dshlocal"
version = "0.1.0"
edition = "2021"
license = "MIT"
links = "tauri-plugin-dshlocal"
build = "build.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
tauri = "2"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
